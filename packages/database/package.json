{
  "name": "@creatorflow/database",
  "version": "0.1.0",
  "private": true,
  "description": "SQL-first database package for CreatorFlow",
  "type": "module",
  "scripts": {
    "migrations:list": "find migrations -type f | sort",
    "migrate:up": "node ./scripts/migrate.mjs up",
    "migrate:status": "node ./scripts/migrate.mjs status",
    "seed:superadmin": "node ./scripts/bootstrap-superadmin.mjs"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
